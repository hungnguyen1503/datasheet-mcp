qdrant-client>=1.9          # Qdrant vector DB client (shared with HUM/Schematic)
fastembed>=0.3              # BM25 sparse embeddings for hybrid search
sentence-transformers>=3.0
mcp[cli]>=1.27,<2.0         # FastMCP structured tool output API range
pydantic>=2.7               # evidence and structured MCP response contracts
python-dotenv               # load .env configuration
tiktoken                    # token counting / budgeting
PyMuPDF>=1.24               # PDF bookmark text extraction (pymupdf fallback)
beautifulsoup4>=4.12        # HTML cleanup in prose extraction
InquirerPy>=0.3.4           # fuzzy TUI multi-select for PDF picker
