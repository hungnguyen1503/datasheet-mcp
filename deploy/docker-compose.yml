services:
  cloudflared:
    image: cloudflare/cloudflared:latest
    container_name: ds-cloudflared
    restart: unless-stopped
    network_mode: host
    command: tunnel --no-autoupdate run
    environment:
      - TUNNEL_TOKEN=${TUNNEL_TOKEN}
